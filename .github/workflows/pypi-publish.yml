name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  semantic-release:
    name: Version, tag, and publish
    runs-on: ubuntu-latest
    environment: release
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install python-semantic-release

      - name: Ensure base tag exists
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          CURRENT_VERSION=$(grep -E '^version\s*=\s*"' pyproject.toml | head -n1 | sed -E 's/version\s*=\s*"([^"]+)"/\1/')
          if ! git rev-parse "v${CURRENT_VERSION}" >/dev/null 2>&1; then
            git tag "v${CURRENT_VERSION}" || true
            git push origin "v${CURRENT_VERSION}" || true
          fi

      - name: Run semantic-release
        env:
          PYPI_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          semantic-release publish
